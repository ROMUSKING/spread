# Scoped Agent Instructions

Application packages: `apps/api` and `apps/web`. Keep all mutations command-mediated. Do not create direct DB writes from web code.

First read: `docs/snapshot-v0.16.1.md` from repository root, then root `AGENTS.md`.
