# Archive

Historical materials live here. Active execution artifacts remain outside `docs/archive/`.

```text
changelog/        older release notes
review/           older critical reviews
spec/             older active specs
snapshot/         older active snapshots
release-artifacts/ validation, manifest, health, and zip-test outputs
```
