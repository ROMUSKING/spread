# Documentation Index

Active first-read artifact:

```text
docs/snapshot-v0.16.1.md
```

Active pack index:

```text
docs/pack-index.md
```

Historical documents and release artifacts live under:

```text
docs/archive/
```

Implementation agents should start with `AGENTS.md`, then the snapshot, then the relevant work order.
