# Scoped Agent Instructions

Tests and fixtures must map to `tests/manifest.yml` evidence URIs. Negative-path fixtures are as important as happy paths.

First read: `../docs/snapshot-v0.16.1.md` from repository root, then root `AGENTS.md`.
