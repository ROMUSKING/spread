# Scoped Agent Instructions

Shared packages. Keep domain, contracts, db, config, observability, UI, and testkit boundaries small and typed. No package may admit post-MVP runtime dependencies without an evidence gate.

First read: `docs/snapshot-v0.18.0.md` from repository root, then root `AGENTS.md`.
